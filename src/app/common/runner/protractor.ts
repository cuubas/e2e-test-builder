import { runner } from 'app/common/runner';
// protractor specific commands
runner.commands.enableSynchronization = function () { };
runner.commands.disableSynchronization = function () { };
runner.commands.callback = function () { };
